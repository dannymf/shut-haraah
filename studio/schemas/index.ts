import blockContent from './blockContent'
import post from './post'
import questionAnswer from './questionAnswer'
import topic from './topic'

export const schemaTypes = [post, topic, blockContent, questionAnswer]
